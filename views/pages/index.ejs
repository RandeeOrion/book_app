<!DOCTYPE html>
<html lang="en">
<head>
  <link href="https://fonts.googleapis.com/css?family=Open+Sans&display=swap" rel="stylesheet">
  <%- include('./partials/head') %>
  <link rel="stylesheet" href="/styles/book.css">
  <title>Sue & Randee Book App</title>
</head>
<body>
  <header>
    <a href="#menu">
      <img src="/images/hamburgerMenu.svg" alt="">Home</a>
      <a href="/searches/new"/>Click Here!</a>
  </header>
    <h2>You have <%= databaseBooks.length %> books on your bookshelf!</h2>
    <ul>
    <% databaseBooks.forEach( book => { %>
      <li>
        <div class="book">
          <img class="image" src="<%= book.image_url %>" alt="<%= book.title %>"/>
          <p class="title">Title:    <%= book.title %></p>
          <p class="authors">Authors:    <%= book.author %></p>
          <p class="authors">ISBN:    <%= book.isbn %></p>
          <a href="/books/<%= book.id %>">Show Details</a>
        </div>
      </li>

    <% }) %>
    </ul>

  <script src="server.js"></script>
</body>
</html>